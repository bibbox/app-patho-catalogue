import { NgModule } from '@angular/core';
import { MockBuilderPromise } from './mock-builder-promise';
import { IMockBuilderResult } from './types';
export declare class MockBuilderPerformance extends MockBuilderPromise {
    build(): NgModule;
    then<TResult1 = IMockBuilderResult, TResult2 = never>(fulfill?: (value: IMockBuilderResult) => PromiseLike<TResult1>, reject?: (reason: any) => PromiseLike<TResult2>): PromiseLike<TResult1 | TResult2>;
    private equalProviders;
    private equalRender;
    private equalsTo;
    private equalVariables;
}
